# Knowledge-Guided Relational Reasoning for Intraoperative Risk Prediction
## Task: Write Evaluation Script for ECML Paper Submission
I need you to write a **single, complete Python file** for evaluating all four phases and final project combining all of my surgical AI system. This is for the **Experiments & Results section** of my ECML research paper submission — it must be accurate, clean, and production-ready, test if it works.

and tell me how to run it. give me the command to run it.

---

## Context

All four phases have been individually trained and their best model are saved by us as you know. I have **not** run `reasoner_demo.py` yet, should i run it? please tell me. The evaluation script must load each saved model and compute the metrics below. We are on **Plan A only**.

---

## Metrics suggested by my mentor

you can add or remove metrics as per your convenience and whatever is best for the paper. these were generated by my mentor and he doesnt have good idea about project.
### Phase 1 & 2 — Structural Perception (Baseline Validation)

Prove that the **Tri-Diff-Transformer (TDT)** outperforms SOTA models (Rendezvous, TriQuery) on CholecT45/T50 benchmark data.

- **Global Triplet mAP (`mAP_IVT`)** — Primary benchmark metric for CholecT45/T50
- **Tail-Class mAP (`mAP_tail`)** — Compute mAP for the rarest 15% of action classes; proves the Tail-Boosted Contrastive Memory Bank works
- **Temporal Phase Edit Distance** — Measures prediction smoothness across Phase 2 workflow; low edit distance = no "flickering," which is critical for clinical usability

---

### Phase 3 — Topological Integrity (Scene Graph Innovation)

Prove the structural correctness of the explicitly generated Scene Graph.

- **Relationship Recall @ K (`R@K`)** — What fraction of ground-truth interactions (e.g., `grasper→retract→gallbladder`) appear in the model's Top-K predicted edges
- **Anatomical Impossible Rate (`AIR`)** *(Critical)* — How often does the model predict a physically impossible triplet (e.g., `Clipper→Clip→Liver`)? Report **with DSR vs. without DSR** to prove the Denoising Semantic Refiner acts as a logical guardrail
- **Active Energy F1-Score** — F1 for the `energy_head`; proves the model correctly distinguishes "hot" (active) vs. "cold" (safe) tool states

---

### Phase 4 — Risk Reasoning (ADS Track Gold Standard)

Prove clinical impact and injury-prevention capability.

- **Critical Risk Recall (`CRR`)** — "Safety Score": what percentage of high-risk events (from `SEED_KNOWLEDGE`) were correctly flagged
- **Nuisance Alert Rate (`NAR`)** — False alarm rate; a low NAR proves the RKG Symbolic Matcher is precise and avoids alarm fatigue in the OR
- **Risk-Weighted mAP (`mAP_RW`)** — Apply a **5× penalty** to errors on "Critical" triplets (e.g., cutting the bile duct) vs. "None"-risk triplets

---

### Cross-Domain Knowledge Metrics

- **SapBERT Alignment Score** — Cosine similarity between Scene Graph edges and Knowledge Graph rules; validates the vision-to-text semantic bridge
- **Explanation Faithfulness** — LLM-based check: does the Phase 4 generated text explanation faithfully reflect the visual features in that frame?

---

## Why These Metrics Matter for ECML

- **Trust**: Surgeons prioritize CRR (Critical Risk Recall) over marginal mAP improvements
- **Innovation**: AIR demonstrates that Phase 3's Denoising Refiner is a "Logical Guardrail," not just an extra layer
- **ADST Criteria**: Risk-Weighted evaluation demonstrates clinical domain understanding beyond standard computer vision benchmarks

---

## Requirements for the Script

- Single `.py` file — clean, well-commented, modular
- Load all saved best-models
- Compute and print all metrics above in a structured, paper-ready format
- Where applicable, produce comparison tables (e.g., **with DSR vs. without DSR** for AIR)
- Plan A only — no fallback paths or alternative implementations